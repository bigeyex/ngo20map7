CREATE TABLE `ngo20map7`.`<table_name>` (
    `id` int NOT NULL,
    `account_id` int,
    `user_id` int,
    `title` varchar(255),
    `step_id` int,
    `form` text NOT NULL,
    PRIMARY KEY (`id`)
) COMMENT='';